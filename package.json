{
  "name": "vizen",
  "version": "0.0.1",
  "description": "A modern static site generator — TS port of zensical (Material for MkDocs) with Vite-powered HMR and client-side navigation",
  "license": "MIT",
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "bin": {
    "vizen": "packages/core/src/cli/index.ts"
  },
  "scripts": {
    "dev": "bun run --filter=./packages/* --filter=. dev",
    "build": "tsc -b && bun run build:all",
    "build:all": "bun run --filter=./packages/* build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@preact/preset-vite": "^2.10.6",
    "@types/bun": "^1.3.0",
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "material-design-color": "^2.3.2",
    "material-shadows": "^3.0.1",
    "sass": "^1.101.0",
    "typescript": "^6.0.0",
    "vite": "^8.0.0",
    "vitest": "^3.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "preact": "^10.29.7",
    "preact-render-to-string": "^6.7.0",
    "smol-toml": "^1.7.0"
  }
}
