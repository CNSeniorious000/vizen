# Welcome

This is the home page of the fixture docs site. It has a [link to getting started](getting-started/).

## Section

Some content with a code block:

```ts
const answer = 42;
console.log(answer);
```
